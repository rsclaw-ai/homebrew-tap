# homebrew-tap

Homebrew tap for [rsclaw](https://github.com/rsclaw-ai/rsclaw).

## Install

```bash
brew tap rsclaw-ai/tap

# CLI
brew install rsclaw

# Desktop app (macOS only)
brew install --cask rsclaw
```

## Upgrade

```bash
brew update && brew upgrade rsclaw
```
